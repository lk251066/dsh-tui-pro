{
  "name": "dsh-tui-pro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^22.19.0 || >=24.0.0",
    "pnpm": "^11.7.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b tsconfig.host.json",
    "lint": "oxlint packages"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "oxlint": "^0.18.0",
    "tsx": "^4.19.2",
    "typescript": "~5.7.3",
    "vitest": "^3.0.5"
  }
}
